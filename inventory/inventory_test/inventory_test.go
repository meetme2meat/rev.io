package inventory_test

import (
	"testing"

	. "github.com/onsi/ginkgo"
)

func TestInventry(t *testing.T) {
	// RegisterFailHandler(Fail)
	RunSpecs(t, "Inventry Test Suite")
}

var _ = BeforeSuite(func() {

})

var _ = AfterSuite(func() {
})
